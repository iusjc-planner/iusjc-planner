x-backend-common: &backend-common
  build:
    context: .
    dockerfile: Dockerfile.backend
  env_file:
    - .env
  environment:
    - DB_HOST=mysql-db
    - DB_PORT=3306
    - DB_NAME=bd_tutore
    - DB_USER=${DB_USERNAME:-root}
    - DB_USERNAME=${DB_USERNAME:-root}
    - DB_PASSWORD=${DB_PASSWORD:-tenzo}
    - DB_PASS=${DB_PASSWORD:-tenzo}
    - EUREKA_HOST=iusj-eureka-service
    - EUREKA_PORT=8761
    - MAIL_ENABLED=${MAIL_ENABLED:-false}
    - MAIL_HOST=${MAIL_HOST:-smtp.gmail.com}
    - MAIL_PORT=${MAIL_PORT:-587}
    - MAIL_USERNAME=${MAIL_USERNAME:-}
    - MAIL_PASSWORD=${MAIL_PASSWORD:-}
    - MAIL_FROM=${MAIL_FROM:-noreply@iusj-planner.cd}
    - MAIL_RESET_URL=${MAIL_RESET_URL:-http://localhost:4200/auth/reset-password}
    - SMS_ENABLED=${SMS_ENABLED:-false}
    - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID:-}
    - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN:-}
    - TWILIO_FROM_NUMBER=${TWILIO_FROM_NUMBER:-}
    - REMINDERS_ENABLED=${REMINDERS_ENABLED:-true}
  depends_on:
    mysql-db:
      condition: service_healthy
    iusj-eureka-service:
      condition: service_started
  networks:
    - iusj-network

services:
  mysql-db:
    image: mysql:8.0
    container_name: iusj-mysql
    restart: always
    environment:
<<<<<<< HEAD
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: iusj_user
      MYSQL_PASSWORD: iusj_password
=======
      MYSQL_DATABASE: bd_tutore
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-tenzo}
>>>>>>> backend
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
<<<<<<< HEAD
      - ./documentation/migrations:/docker-entrypoint-initdb.d
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # SERVICE DISCOVERY (EUREKA)
  # ============================================
  eureka:
    build:
      context: ./iusj-eureka-service
      dockerfile: Dockerfile
    container_name: iusj-eureka
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_INSTANCE_HOSTNAME=eureka
      - EUREKA_CLIENT_REGISTER_WITH_EUREKA=false
      - EUREKA_CLIENT_FETCH_REGISTRY=false
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # API GATEWAY
  # ============================================
  gateway:
    build:
      context: ./iusj-gateway-service
      dockerfile: Dockerfile
    container_name: iusj-gateway
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=gateway
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # AUTHENTICATION SERVICE
  # ============================================
  auth-service:
    build:
      context: ./iusj-auth-service
      dockerfile: Dockerfile
    container_name: iusj-auth-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=auth-service
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRATION=${JWT_EXPIRATION}
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # USER SERVICE
  # ============================================
  user-service:
    build:
      context: ./iusj-user-service
      dockerfile: Dockerfile
    container_name: iusj-user-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=user-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # TEACHER SERVICE
  # ============================================
  teacher-service:
    build:
      context: ./iusj-teacher-service
      dockerfile: Dockerfile
    container_name: iusj-teacher-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=teacher-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # ROOM SERVICE
  # ============================================
  room-service:
    build:
      context: ./iusj-room-service
      dockerfile: Dockerfile
    container_name: iusj-room-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=room-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # COURSE SERVICE
  # ============================================
  course-service:
    build:
      context: ./iusj-course-service
      dockerfile: Dockerfile
    container_name: iusj-course-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=course-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # SCHEDULE SERVICE
  # ============================================
  schedule-service:
    build:
      context: ./iusj-schedule-service
      dockerfile: Dockerfile
    container_name: iusj-schedule-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=schedule-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # SCHOOL SERVICE
  # ============================================
  school-service:
    build:
      context: ./iusj-school-service
      dockerfile: Dockerfile
    container_name: iusj-school-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=school-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # GROUP SERVICE
  # ============================================
  group-service:
    build:
      context: ./iusj-group-service
      dockerfile: Dockerfile
    container_name: iusj-group-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=group-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # STUDENT SERVICE
  # ============================================
  student-service:
    build:
      context: ./iusj-student-service
      dockerfile: Dockerfile
    container_name: iusj-student-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=student-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # RESOURCE SERVICE
  # ============================================
  resource-service:
    build:
      context: ./iusj-resource-service
      dockerfile: Dockerfile
    container_name: iusj-resource-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=resource-service
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # NOTIFICATION SERVICE
  # ============================================
  notification-service:
    build:
      context: ./iusj-notification-service
      dockerfile: Dockerfile
    container_name: iusj-notification-service
    restart: always
    depends_on:
      - mysql
      - eureka
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - EUREKA_INSTANCE_HOSTNAME=notification-service
      - MAIL_HOST=${MAIL_HOST}
      - MAIL_PORT=${MAIL_PORT}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # FRONTEND (ANGULAR + NGINX)
  # ============================================
  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: iusj-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - gateway
    environment:
      - API_URL=http://gateway:8080
    networks:
      - iusj-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

# ============================================
# VOLUMES
# ============================================
volumes:
  mysql_data:
    driver: local

# ============================================
# NETWORKS
# ============================================
networks:
  iusj-network:
    driver: bridge
=======
    networks:
      - iusj-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 10s
      retries: 5

  # Registry
  iusj-eureka-service:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-eureka-service
    container_name: iusj-eureka-service
    ports:
      - "8761:8761"
    environment:
      - EUREKA_HOST=iusj-eureka-service
    networks:
      - iusj-network

  # Core Services
  iusj-auth-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-auth-service

  iusj-course-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-course-service

  iusj-event-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-event-service

  iusj-group-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-group-service

  iusj-notification-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-notification-service

  iusj-report-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-report-service
    environment:
      - DB_HOST=mysql-db
      - DB_PORT=3306
      - DB_NAME=bd_tutore
      - DB_USER=${DB_USERNAME:-root}
      - DB_USERNAME=${DB_USERNAME:-root}
      - DB_PASSWORD=${DB_PASSWORD:-tenzo}
      - DB_PASS=${DB_PASSWORD:-tenzo}
      - EUREKA_HOST=iusj-eureka-service
      - EUREKA_PORT=8761
      - REPORT_STORAGE_PATH=/tmp/reports
      - MAIL_ENABLED=${MAIL_ENABLED:-false}
      - MAIL_HOST=${MAIL_HOST:-smtp.gmail.com}
      - MAIL_PORT=${MAIL_PORT:-587}
      - MAIL_USERNAME=${MAIL_USERNAME:-}
      - MAIL_PASSWORD=${MAIL_PASSWORD:-}
      - MAIL_FROM=${MAIL_FROM:-noreply@iusj-planner.cd}
    volumes:
      - iusj_reports:/tmp/reports

  iusj-resource-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-resource-service

  iusj-room-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-room-service

  iusj-schedule-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-schedule-service

  iusj-school-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-school-service

  iusj-student-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-student-service

  iusj-teacher-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-teacher-service

  iusj-user-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-user-service

  # Gateway (Needs to be started after Eureka)
  iusj-gateway-service:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        SERVICE_NAME: iusj-gateway-service
    container_name: iusj-gateway-service
    ports:
      - "8080:8080"
    depends_on:
      iusj-auth-service:
        condition: service_started
      iusj-user-service:
        condition: service_started
    # (other services are accessed via LB, so just need eureka to route)

  # Frontend Angular
  web:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: iusj-frontend
    ports:
      - "80:80"
    depends_on:
      - iusj-gateway-service
    networks:
      - iusj-network

networks:
  iusj-network:
    driver: bridge

volumes:
  mysql_data:
  iusj_reports:
>>>>>>> backend
